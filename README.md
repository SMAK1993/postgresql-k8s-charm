# postgresql-k8s-charm
A Postgres charm meant to be deployed on kubernetes

Build the charm with charmcraft build (can be obtained with snap install charmcraft --edge)

Deploy the charm with juju deploy ./postgresql.charm